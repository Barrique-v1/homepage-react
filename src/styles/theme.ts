import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#1976d2', // Blau
        },
        secondary: {
            main: '#dc004e', // Rot
        },
    },
});

export default theme;
